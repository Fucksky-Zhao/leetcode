class Solution {
    public int bulbSwitch(int n) {
        // int res = 0;
        // for (int i = 1; i <= n; i++) {
        //     int a = (int) Math.sqrt(i);
        //     if (a * a == i) res++;
        // }
        // return res;
        return (int) Math.sqrt(n);
    }
}
